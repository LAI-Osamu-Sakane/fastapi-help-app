#[project]
#name = "fastapi-help-app"
#version = "0.1.0"
#requires-python = ">=3.10"
#dependencies = []
[project]
name = "fastapi-help-app"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "fastapi",
    "python-socketio>=5.16.2",
    "uvicorn",
]
